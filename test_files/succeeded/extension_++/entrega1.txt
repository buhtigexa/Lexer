{
uint  i, j,  l, m;
long a, b, c, d, e, f,x,k,g; *(se define a,g y k como long,pues no estaba)*

print('comienzo de programa');

*( Esto es
un comentario
largo )*

*( uint)*
i:= 65535;
j := 65534;
k := 70000;

*( long)*
 *( a los valores fuera de rango se les disminuye en 1, para evitar el error)*
d:=-80000;
a:=-2147483647;
b:=-2147483647;
b:=2147483647;
c:=2147483647;

e:=tolong(5);
e:=-5;
e:=-6;
e:=-7;
e:=tolong(7);

{ uint a, b;
  c:=tolong(tolong(a+b)*tolong(j));   *(se agrego tolong (j): j es uint )*
}
if (a>b)
  i:=242-3+8;   *( se quitan los paréntesis de la expresion: (242-3)+8)*
 else
  x:=a;

{ uint i, j;
  long l;       *(se define l como long para no hacer cambio dentro del 
		while)*
if(i <= 10)
      	{
       	if (i<>0)
        	if (i=3)
             		c:=tolong(2); *(se convierte a long 1,2,3,y 10 )*
               	else
		{
               		c:=tolong(1);
       			g:=g+tolong(10);
       	*( esta linea se marca como comentario		j:=touint(a+e*2); )*
			k:=tolong(c+b*d-tolong(1)/e+a*tolong(5)); *(se cambia touint por tolong)*
		}
	else
	c:=tolong(3);
	}
else
do
  {
   a:=b-c-d;
   l := e - tolong(1);
  }
  while(e > tolong(1));
}

*( Otro comentario  )*
}